solution for codewars problem 'Multiply Word in String'

function takes a string, an index, and a number. returns the word at index repeated (number) amount of times w/ a hyphen between each repetiion.